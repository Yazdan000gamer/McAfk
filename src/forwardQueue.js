'use strict';
const CFG=require('./config');let bot=null;const pending=new Map();const attachTelegramBot=b=>{bot=b};const flush=id=>{const e=pending.get(id);if(!e)return;pending.delete(id);clearTimeout(e.timer);if(bot&&e.lines.length)bot.api.sendMessage(id,e.lines.join('\n')).catch(()=>{})};const enqueueForward=(key,line)=>{const store=require('./store'),groups=store.forwardMap.get(key);if(!groups?.size)return;for(const id of groups){let e=pending.get(id);if(!e){e={lines:[],timer:setTimeout(()=>flush(id),CFG.FORWARD_BATCH_MS)};e.timer.unref?.();pending.set(id,e)}e.lines.push(line);if(e.lines.length>=CFG.FORWARD_BATCH_MAX_LINES||e.lines.reduce((n,x)=>n+x.length+1,0)>=CFG.FORWARD_BATCH_MAX_CHARS)flush(id)}};module.exports={enqueueForward,attachTelegramBot,flush};
