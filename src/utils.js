'use strict';const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');const stripColors=v=>String(v??'').replace(/§[0-9a-fk-or]/gi,'').replace(/\x1b\[[0-9;]*m/g,'').trim();const normUsername=v=>String(v||'').replace(/^@/,'').toLowerCase();const uptime=m=>{let s=Math.max(0,Math.floor(m/1000)),mi=Math.floor(s/60),h=Math.floor(mi/60),d=Math.floor(h/24);return d?`${d}d ${h%24}h`:h?`${h}h ${mi%60}m`:mi?`${mi}m ${s%60}s`:`${s}s`};const dot=s=>s==='online'?'🟢':s==='connecting'?'🟡':s==='error'?'🔴':'⚫';const ownerTag=i=>i.ownerUsername?`@${i.ownerUsername}`:i.userId?`ID ${i.userId}`:`#${i.chatId}`;const hashId=x=>{let h=2166136261;for(const c of String(x)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0};const botIdFor=(n,h,p)=>String(hashId(`${n}:${h}:${p}`));const statusLabel=s=>s==='online'?'ONLINE':s==='connecting'?'CONNECTING':s==='error'?'ERROR':'OFFLINE';const shortBotName=n=>String(n||'').length>18?`${String(n).slice(0,17)}…`:String(n||'');const reconnectDelay=(a,c)=>Math.min(c.BASE_RECONNECT_MS*2**Math.max(0,a-1),c.MAX_RECONNECT_MS);const safeJsonParse=(v,f)=>{try{return JSON.parse(v)}catch{return f}};const formatMinecraftReason=r=>stripColors(typeof r==='string'?r:r?.text||r?.translate||JSON.stringify(r||''));module.exports={esc,stripColors,normUsername,uptime,dot,ownerTag,hashId,botIdFor,statusLabel,shortBotName,reconnectDelay,safeJsonParse,formatMinecraftReason};
